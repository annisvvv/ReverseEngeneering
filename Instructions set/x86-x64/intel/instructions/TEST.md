test is a logical compare, computes the bit-wise AND from [[AND - OR - XOR - NOT]] of the first operand and the second operand and sets the SF, ZF, and PF status flags according to the result. 

The test doesn't store any result.