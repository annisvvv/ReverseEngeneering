NOP means no operations, no registers, no values. its there to pad or align bytes or delay time.

NOP is like a pseudo instruction of `addi x0, x0, 0` means `zero reggister = zero register + 0`

`c.nop` is `c.addi`