unlike most assembly instructions stores are read from left to right like sd.

parenthesis in the disassembly indicate the use of an address as a memory address whether for reading or writing memory.

when looking at memory address of the stack if it goes down by 2byte then it means there is compressed instructions.