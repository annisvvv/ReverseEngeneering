These two wither add or subtract something, the destination can either be r/mX or register the source operand can either be r/mX or register or immediate.

note that no source and destination as r/mXs because that could allow memory to memory, which isn't allowed in x86.